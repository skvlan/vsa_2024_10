server {
    listen 80 default_server;
    listen 443 default_server;

    server_name carmarket;

    location /static/ {
        alias /carmarket/src/static/;
    }

    location /media/ {
        alias /carmarket/src/media/;
    }

    location / {
        proxy_set_header Host $host;
        proxy_pass http://backend:8000;
    }
}